version: "3"
services:
  web:
    build: .
    ports:
      - "8080:80"
#    volumes:
#     - ./web:/code # modified here to take into account the new app path
    environment:
      - NODE_ENV=production